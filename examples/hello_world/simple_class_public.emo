class Person {
  var name: String
  public var age: Int

  const maxAge = 35

  func tooOld?() {
    return age > maxAge
  }

  private
  func olderThan?(max: Int) -> Bool {
    return self.age > max
  }
}
